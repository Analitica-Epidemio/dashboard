# Configuracion de Deploy SSH
# Copia este archivo a .env.deploy y configura los valores
#
# El script deploy.sh lee primero .env.deploy, y si no existe usa .env
#
# Uso:
#   cp .env.deploy.example .env.deploy
#   # Editar valores
#   make deploy

# =============================================================================
# APLICACION
# =============================================================================
# Nombre de la aplicacion (se usa para nombrar containers, volumenes, etc)
APP_NAME=epidemiologia

# =============================================================================
# CONEXION SSH
# =============================================================================
# Host del servidor (IP o hostname)
SSH_HOST=192.168.1.100

# Usuario SSH
SSH_USER=deploy

# Puerto SSH (default: 22)
SSH_PORT=22

# Path a clave privada SSH (opcional, usa default si esta vacio)
# SSH_KEY=~/.ssh/id_rsa_deploy

# Directorio remoto (default: /opt/${APP_NAME})
# REMOTE_DIR=/opt/epidemiologia

# =============================================================================
# VARIABLES DE PRODUCCION
# Estas se copian al servidor para el build de Docker
# =============================================================================

# Base de datos (PostgreSQL)
DB_NAME=epidemiologia_db
DB_USER=epidemiologia_user
DB_PASSWORD=CAMBIAR_ESTO_PASSWORD_SEGURO

# Seguridad - genera con: openssl rand -hex 32
SECRET_KEY=CAMBIAR_ESTO_GENERAR_CON_OPENSSL

# URLs - ajustar segun el servidor/dominio
# Con Nginx como proxy (lo normal):
NEXT_PUBLIC_API_HOST=http://192.168.1.100/api
FRONTEND_URL=http://192.168.1.100

# CORS - debe incluir el frontend URL
CORS_ORIGINS=http://192.168.1.100

# Hosts permitidos - incluir IP/dominio del servidor
ALLOWED_HOSTS=192.168.1.100,localhost

# =============================================================================
# APIS EXTERNAS (opcional)
# =============================================================================
# Google Maps API Key (para geocodificacion)
# GOOGLE_MAPS_API_KEY=AIza...

# Mapbox (alternativa para mapas)
# MAPBOX_ACCESS_TOKEN=pk.ey...
